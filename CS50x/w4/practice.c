#include<stdio.h>

int main(void)
{

}

 // int n = 50; // By declaring n we get some memory for both its value and n, thus n is just a pointer to its value stored somewhere else in memory. Pointers, being variables, also live in memory and need some space.
   // int *p = &n; // Declaring pointer p and assigning the n's address to it. If we don't assign an address or NULL a random value will be assigned (a leftover from previous use of this memory cell)
   // printf("%p\n", p);